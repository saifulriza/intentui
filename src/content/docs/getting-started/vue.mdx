---
title: "Vue 3"
description: Use Intent UI in Vue 3 with native Vue components (Phase 4) without React bridge dependencies.
---

## Installation
Create a Vue project with Vite:
<InstallCommand command="npm create vite@latest"/>

Install Tailwind CSS and Vue plugin:
<InstallCommand command="npm install tailwindcss @tailwindcss/vite @vitejs/plugin-vue"/>

## Configure Vite
Update your `vite.config.ts`:
```ts
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [vue(), tailwindcss()],
})
```

## Phase 4 components (Vue-native)
Phase 4 includes foundational + form + utility + identity/navigation components without React dependencies:
- `Button.vue`
- `Badge.vue`
- `Card.vue`
- `CardHeader.vue`
- `CardContent.vue`
- `CardFooter.vue`
- `Input.vue`
- `InputGroup.vue`
- `Checkbox.vue`
- `CheckboxGroup.vue`
- `Radio.vue`
- `RadioGroup.vue`
- `Textarea.vue`
- `Separator.vue`
- `Keyboard.vue`
- `Loader.vue`
- `Avatar.vue`
- `Link.vue`
- `Switch.vue`

You can copy these from:
`/src/vue/components/ui`

## Use in Vue
Use the components directly in `.vue` files with `v-model`:
```vue
<script setup lang="ts">
import { ref } from 'vue'
import Button from '@/components/ui/Button.vue'
import Badge from '@/components/ui/Badge.vue'
import Card from '@/components/ui/Card.vue'
import CardHeader from '@/components/ui/CardHeader.vue'
import CardContent from '@/components/ui/CardContent.vue'
import CardFooter from '@/components/ui/CardFooter.vue'
import Input from '@/components/ui/Input.vue'
import Checkbox from '@/components/ui/Checkbox.vue'
import CheckboxGroup from '@/components/ui/CheckboxGroup.vue'
import Radio from '@/components/ui/Radio.vue'
import RadioGroup from '@/components/ui/RadioGroup.vue'
import Textarea from '@/components/ui/Textarea.vue'
import Separator from '@/components/ui/Separator.vue'
import Loader from '@/components/ui/Loader.vue'
import Keyboard from '@/components/ui/Keyboard.vue'
import Avatar from '@/components/ui/Avatar.vue'
import Link from '@/components/ui/Link.vue'
import Switch from '@/components/ui/Switch.vue'

const name = ref('')
const channels = ref<string[]>(['email'])
const plan = ref('starter')
</script>

<template>
  <Card>
    <CardHeader title="Vue 3 Native" description="Phase 4 components" />
    <CardContent>
      <Input v-model="name" placeholder="Your name" />
      <div class="mt-4">
        <Textarea v-model="name" placeholder="Short bio" />
      </div>
      <div class="mt-4 space-y-3">
        <CheckboxGroup v-model="channels">
          <Checkbox value="email" label="Email" />
          <Checkbox value="sms" label="SMS" />
        </CheckboxGroup>
      </div>
      <div class="mt-4 space-y-3">
        <RadioGroup v-model="plan">
          <Radio value="starter" label="Starter" />
          <Radio value="pro" label="Pro" />
        </RadioGroup>
      </div>
      <div class="mt-4">
        <Badge intent="success">Selected plan: {{ plan }}</Badge>
      </div>
      <div class="mt-4 flex items-center gap-2">
        <Keyboard>⌘K</Keyboard>
        <Loader />
      </div>
      <div class="mt-4 flex items-center gap-3">
        <Avatar initials="IN" alt="Intent UI" />
        <Link href="https://intentui.com" target="_blank" rel="noreferrer">Visit docs</Link>
      </div>
      <div class="mt-4">
        <Switch :model-value="true" label="Notifications" />
      </div>
      <div class="mt-4">
        <Separator />
      </div>
    </CardContent>
    <CardFooter>
      <Button intent="primary">Continue</Button>
    </CardFooter>
  </Card>
</template>
```

## Notes
- These Phase 4 components are Vue-native and do not depend on React, React DOM, or Veaury.
- More interactive components (menu/dialog/select/date, etc.) will follow in next phases.

## Other frameworks
You might be interested in using Intent with other frameworks.
<FrameworkGuides/>
